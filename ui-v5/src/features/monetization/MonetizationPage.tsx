import React from "react";
import { useQuery } from "@tanstack/react-query";
import { Coins } from "lucide-react";
import { researchTimeline, runSummary } from "../../shared/researchApi";

export const MonetizationPage: React.FC = () => { const t=useQuery({queryKey:["research-timeline"],queryFn:researchTimeline}); const s=useQuery({queryKey:["run-summary"],queryFn:runSummary}); const photos=t.data?.photos??[]; if(t.isLoading||s.isLoading)return <div className="p-8 text-slate-300">Загрузка данных…</div>; return <div className="flex flex-col h-[calc(100vh-49px)] w-full bg-[#080d12] text-[#e2e8f0] overflow-y-auto p-6 space-y-5"><header className="rounded-lg border border-amber-800/70 bg-[#0b1117] p-5"><div className="flex items-center gap-2 font-mono text-sm font-bold text-amber-300"><Coins className="h-5 w-5"/> АРТЕФАКТЫ И ПРОВЕНАНС · СТАТУС РЕАЛЬНЫХ ДАННЫХ</div><p className="text-xs text-slate-300 mt-2">В API нет объектов публикации, токенизации или тарифов. Демонстрационные карточки удалены; интерфейс показывает только то, что подтверждено локальным исследовательским запуском.</p></header><section className="grid grid-cols-2 md:grid-cols-4 gap-3">{[["Фото Stage 2",photos.length],["Источник",s.data?.source_mode],["Пары",photos.reduce((n,p)=>n+(p.stage2PairCount ?? 0),0)],["Артефакты", "н/д"]].map(([k,v])=><div key={String(k)} className="rounded-lg border border-[#1f2d3d] bg-[#0b1117] p-4"><div className="text-xs text-slate-500">{k}</div><div className="mt-2 text-lg font-mono text-cyan-300">{v ?? "н/д"}</div></div>)}</section><section className="rounded-lg border border-[#1f2d3d] bg-[#0b1117] p-5"><h2 className="font-mono text-xs text-cyan-300">ПУБЛИКАЦИЯ И ХРАНЕНИЕ</h2><div className="mt-3 rounded border border-[#1f2d3d] bg-[#101820] p-4 text-sm text-slate-300">Реальный локальный источник: <code>{s.data?.source_mode ?? "н/д"}</code>. В API нет подключения внешнего хранилища, публикационного сервиса или платёжных объектов. Поэтому цены, количество токенов и адреса хранения не отображаются.</div></section></div>; };
