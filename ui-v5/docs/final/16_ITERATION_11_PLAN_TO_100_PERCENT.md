# ПЛАН РАБОТ НА СЛЕДУЮЩУЮ ИТЕРАЦИЮ (ИТЕРАЦИЯ 11: БОЕВАЯ ИНТЕГРАЦИЯ ДО 100% ГОТОВНОСТИ)

**Дата составления:** 2026-08-05  
**Текущая оценка готовности:** **93.0 / 100 баллов** (согласно аудиту 18 факторов ТЗ).  
**Целевая оценка после Итерации 11:** **100.0 / 100 баллов** (100% готовность рабочей станции `DEEPUTIN UI v5.0` к боевому извлечению, калибровке, статистическому анализу и публикации результатов расследования по архиву из 1,900 фотографий 1999–2026 гг.).  
**Связанные документы:** [`../FORENSIC_EXPERT_99_LEVEL_PROJECT_REVIEW_AND_UI_V5_PLAN.md`](../FORENSIC_EXPERT_99_LEVEL_PROJECT_REVIEW_AND_UI_V5_PLAN.md), `01_FINAL_REMEDIATION_PLAN.md`.

---

## 1. АРХИТЕКТУРА СКВОЗНОГО ПОТОКА ДАННЫХ (END-TO-END LIVE PIPELINE)

Итерация 11 переключает клиентское приложение `ui-v5` с демонстрационного набора данных (`mockData.ts`) на живой сервер `app6/api/server.py` и децентрализованный архив.

```text
+---------------------------------------------------------------------------------------------------+
|                        СКВОЗНОЙ ПОТОК ДАННЫХ В ИТЕРАЦИИ 11 (93% -> 100%)                          |
|                                                                                                   |
|  [ АРХИВ 1900 ФОТО ] ---> [ app6/api/server.py ] <=== REST API /api/v1 ===> [ UI v5 WORKSTATION ] |
|  (IPFS/Arweave RAW)       (FastAPI + 3DDFA_v3)     (React Query + Cache)    (12 модулей, Canvas 3D)|
|                                    |                                                |             |
|                                    +---> [ BFM Mesh .npy/.npz ] --------------------+             |
|                                    +---> [ UV Albedo 1024x1024 ] -------------------+             |
|                                    +---> [ LOPO 7/7 Health JSON ] ------------------+             |
+---------------------------------------------------------------------------------------------------+
```

---

## 2. ДЕТАЛЬНЫЙ ПАКЕТ РАБОТ НА ИТЕРАЦИЮ 11 (4 КЛЮЧЕВЫХ ЭПИКА)

Работа разбита на 4 инженерных эпика с четким распределением баллов, затрагиваемых факторов (F1–F18), файлов-целей и критериев приемки (`Acceptance Gates`).

### ЭПИК 11.1: Live API Client & React Query Layer
- **Цель:** Подключить фронтенд к боевым эндпоинтам `app6/api/server.py` (порт 8000) с клиентским кэшированием и поддержкой работы в офлайн-режиме.
- **Вклад в итоговую оценку:** **+3.5 балла** (Факторы **F2, F18**).
- **Файлы для изменения/создания:**
  - `ui-v5/src/shared/api.ts` — реализация строго типизированных REST-методов.
  - `ui-v5/src/shared/queries.ts` — хуки `@tanstack/react-query` (`useTimelineQuery`, `usePhotoInfoQuery`, `usePairMetricsQuery`, `useCalibrationHealthQuery`).
  - `ui-v5/vite.config.ts` — верификация проксирования `/api/v1` на `http://127.0.0.1:8000`.
- **Конкретные задачи:**
  1. Реализовать асинхронный адаптер для получения списка точек таймлайна: `GET /api/v1/timeline?pose={pose_code}`.
  2. Настроить загрузку миниатюр из эндпоинтов: `GET /api/v1/photos/{id}/image?kind=thumbnail` с fallback-кэшированием в IndexedDB браузера.
  3. Подключить чтение метаданных: `GET /api/v1/photos/{id}/info_keys` и `GET /api/v1/photos/{id}/skin_zones` для заполнения «Компактной панели фактов» в Инспекторе.
  4. Подключить матрицу расстояний: `GET /api/v1/pairs/{photo_a}/{photo_b}/metrics` для Pair Analysis.
  5. Настроить автообновление статуса калибровки: `GET /api/v1/calibration/health` и `GET /api/v1/system/health`.
- **Критерий приемки Эпика 11.1:** При выборе ракурсной корзины в `TopBar` приложение делает реальный HTTP-запрос к бэкенду, получает валидный JSON по контракту `confounders_v1.json` и отображает фотографии без использования `mockData.ts`.

---

### ЭПИК 11.2: DOM-виртуализация 1,900 записей и Web Worker для Pair Analysis
- **Цель:** Обеспечить стабильные **60 FPS** в браузере при работе с полным архивом из 1,900 фотографий без перегрузки DOM-дерева и блокировок UI-потока.
- **Вклад в итоговую оценку:** **+2.0 балла** (Фактор **F12**).
- **Файлы для изменения/создания:**
  - `ui-v5/src/features/data-manager/DataManagerPage.tsx` — интеграция `@tanstack/react-virtual` в таблицу из 1,900 строк.
  - `ui-v5/src/features/pair-analysis/PairAnalysisPage.tsx` — виртуализация 4-рядной ленты миниатюр (до 1900 фото в сетке).
  - `ui-v5/src/workers/pairDistance.worker.ts` — фоновый Web Worker для пересчета оверлеев.
- **Конкретные задачи:**
  1. Внедрить хук `useVirtualizer` из `@tanstack/react-virtual` в `DataManagerPage.tsx`, чтобы в DOM рендерились только ~25 видимых строк таблицы плюс буфер из 10 элементов сверху и снизу.
  2. Внедрить сеточный виртуализатор в ленту `PairAnalysisPage.tsx`, поддерживающий динамический ресайз размера миниатюр (`40px -> 96px`).
  3. Реализовать Web Worker (`pairDistance.worker.ts`), который при клике пользователя на **Фото А** асинхронно вычисляет расстояние Махаланобиса/SNR для всех 1,900 миниатюр и возвращает карту оверлеев (светло-зеленый 20% / светло-красный 20%) менее чем за **30 мс**.
- **Критерий приемки Эпика 11.2:** Скролл таблицы на 1900 строк и 4-рядной ленты миниатюр работает без рывков; профилировщик Chrome DevTools фиксирует время кадра $< 16.6\text{ мс}$ (60 FPS); потребление памяти браузером не превышает 250 МБ.

---

### ЭПИК 11.3: Загрузка боевой 3D-топологии BFM (`.npy/.npz`) в `FaceMesh3D.tsx`
- **Цель:** Переключить интерактивный 3D-рендерер `FaceMesh3D.tsx` с аппроксимированной топологии на реальные 3DDFA_v3 массивы костной геометрии и UV-альбедо кожи.
- **Вклад в итоговую оценку:** **+1.5 балла** (Факторы **F2, F7**).
- **Файлы для изменения/создания:**
  - `ui-v5/src/shared/ui/FaceMesh3D.tsx` — подключение загрузчика бинарных `.npy/.npz` массивов.
  - `app6/api/server.py` — проверка работы эндпоинта `GET /api/v1/photos/{id}/mesh`.
  - `ui-v5/src/features/photo-inspector/PhotoInspectorPage.tsx` — передача боевого URL меша.
  - `ui-v5/src/features/morphing/MorphingPage.tsx` — интерполяция боевых костных векторов при морфинге.
- **Конкретные задачи:**
  1. Добавить в `FaceMesh3D.tsx` функцию бинарного парсинга `raw object-normalized coordinates`, возвращаемых эндпоинтом `/api/v1/photos/{id}/mesh`.
  2. Настроить наложение реальной 1024×1024 UV-текстуры из `GET /api/v1/photos/{id}/image?kind=uv_texture` на полигональный каркас BFM.
  3. Отрисовать точные координаты **Subset-91** (91 стабильная костная вершина) в виде контрастных зеленых 3D-сфер на основе индексов из `app6/api/key_catalog.py`.
  4. Подключить расчет шейдера тепловой карты в Морфинге к реальному вектору отклонения костных вершин между Фото А и Фото В ($\Delta V_i$).
- **Критерий приемки Эпика 11.3:** В «Инспекторе фотографии» и «Морфинге» отображается точная 3D-модель выбранного архивного снимка, совпадающая по геометрии и текстуре со слоем Stage 1 бэкенда; чекбокс «Ключевые точки» подсвечивает ровно 91 вершину.

---

### ЭПИК 11.4: E2E-верификация на боевом сервере и Immutable Bundle Release
- **Цель:** Провести полное автоматизированное регрессионное тестирование связки бэкенда и фронтенда, зафиксировать Immutable Release Bundle и подписать итоговую декларацию 100% готовности.
- **Вклад в итоговую оценку:** **Финальный Release Gate 100%**.
- **Файлы для изменения/создания:**
  - `ui-v5/e2e/live-integration.spec.ts` — E2E тесты с реальным API.
  - `RUN_PROJECT.sh` — проверка сквозного запуска `RUN_PROJECT.sh api` и `RUN_PROJECT.sh check`.
  - `docs/final/17_RELEASE_DECLARATION_100_PERCENT.md` — итоговая декларация выпуска.
- **Конкретные задачи:**
  1. Выполнить совместный запуск сервера бэкенда (`app6/api/server.py` на порту 8000) и клиентского приложения (`ui-v5` на порту 4175).
  2. Запустить полный тестовый прогон:
     - 97 Python-тестов (`./RUN_PROJECT.sh check`);
     - 15 Unit/Integration тестов фронтенда (`cd ui-v5 && npm test -- --run`);
     - Playwright E2E тесты (`ui-v5/e2e/`).
  3. Сгенерировать финальный зашифрованный архив **Immutable Release Bundle** (манифесты, код, конфиги, хеши SHA-256).
- **Критерий приемки Эпика 11.4:** 100% тестов проходят без единой ошибки или предупреждения; архив готов к передаче журналисту-расследователю.

---

## 3. МАТРИЦА РИСКОВ ИТЕРАЦИИ 11 И МЕРЫ СМЯГЧЕНИЯ (RISK MITIGATION)

| # | Наименование риска | Вероятность | Влияние | Стратегия смягчения (Mitigation Strategy) |
|---|---|:---:|:---:|---|
| **R1** | Задержки сети при одновременной загрузке 1900 миниатюр в ленту Pair Analysis | Средняя | Высокое | Использование легковесных WebP-тайлов 40×40px (< 2 КБ на файл) + агрессивное клиентское кэширование в IndexedDB браузера с TTL 24 часа. |
| **R2** | Падение FPS в браузере при рендеринге сложных 3D-мешей на ноутбуках со слабой графикой | Низкая | Среднее | Внедрение механизма **GPU Budget Control**: мониторинг FPS в реальном времени; если FPS < 30, система автоматически упрощает шейдер тепловой карты. |
| **R3** | Контаминация порогов при настройке калибровки смещения гипотез (`Shift Bias X/Y/Z`) | Низкая | Критическое | Жесткий системный запрет на экспорт слайдеров из раздела Валидации гипотез в публичные отчеты; проверка нулевой гипотезы $H_0$ только по эталонам LOPO 7/7. |
| **R4** | Ошибочная атрибуция даты журналистом в архивных файлах 1999–2005 гг. | Высокая | Среднее | Автоматический флаг `конфликт дат · EXIF ≠ имя` в таблице Данные и Provenance; обязательная визуальная сверка в боковом Sidecar-инспекторе. |

---

## 4. ГРАФИК И ПОСЛЕДОВАТЕЛЬНОСТЬ ВЫПОЛНЕНИЯ (WORKFLOW SCHEDULE)

```text
===================================================================================================
 ДЕНЬ 1 (ШАГ 1)              ДЕНЬ 2 (ШАГ 2)              ДЕНЬ 3 (ШАГ 3)              ДЕНЬ 4 (РЕЛИЗ)
===================================================================================================
[ ЭПИК 11.1 ]               [ ЭПИК 11.2 ]               [ ЭПИК 11.3 ]               [ ЭПИК 11.4 ]
Live API Client &           DOM-виртуализация 1900     Загрузка .npy/.npz          E2E-тесты на боевом
React Query хуки            строк + Web Worker          топологии в FaceMesh3D     сервере + Release 100%
===================================================================================================
```

---

## 5. ЧЕК-ЛИСТ ФИНАЛЬНОЙ ПРИЕМКИ 100% (DEFINITION OF DONE)

- [ ] **1. API-контракты:** `shared/api.ts` успешно получает реальные JSON-манифесты с сервера `app6/api/server.py`.
- [ ] **2. Виртуализация:** Таблица из 1,900 строк в `/data-manager` и 4-рядная лента в `/pair-analysis` скроллят при стабильных 60 FPS.
- [ ] **3. Web Worker:** Выбор Фото А в Pair Analysis пересчитывает 1,900 оверлеев сходства в фоновом потоке за $< 30\text{ мс}$.
- [ ] **4. 3D-топология:** Инспектор фотографии и Морфинг отображают настоящую BFM-модель (`3DDFA_v3`) и альбедо-текстуру архивного снимка.
- [ ] **5. Ключевые точки:** Чекбокс «Ключевые точки» отрисовывает ровно 91 анатомическую вершину (Subset-91) на 3D-меше.
- [ ] **6. Тесты бэкенда:** `./RUN_PROJECT.sh check` выполняет 97 тестов за $< 1\text{ с}$ со статусом `100% PASSED`.
- [ ] **7. Тесты фронтенда:** `npm test -- --run` выполняет 15 unit/integration тестов со статусом `100% PASSED`.
- [ ] **8. TypeScript:** `npm run typecheck` в `ui-v5/` выдает 0 ошибок в `strict: true`.
- [ ] **9. Сборка Vite:** `npm run build` собирает продакшн-бандл без критических ошибок.
- [ ] **10. Декларация 100%:** Подписан финальный акт приемки `17_RELEASE_DECLARATION_100_PERCENT.md`, подтверждающий оценку **100.0 / 100 баллов** по всем 18 факторам ТЗ.
