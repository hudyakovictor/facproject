import React from "react";
import { useTimeline, useRunSummary } from "../../shared/api/queries";
import { Lock, AlertTriangle } from "lucide-react";

export const AuditLogPage: React.FC = () => {
  const t = useTimeline();
  const s = useRunSummary();
  const photos = t.data?.photos ?? [];
  return <div className="flex flex-col h-workspace w-full bg-surface-canvas text-ink-primary overflow-y-auto p-6 space-y-5"><header className="rounded-lg border border-cyan-600 bg-surface-base p-5"><div className="flex items-center gap-2 font-mono text-sm font-bold text-cyan-300"><Lock className="h-5 w-5"/> ПРОВЕНАНС И СТАТУС ИССЛЕДОВАТЕЛЬСКОГО ЗАПУСКА</div><p className="text-xs text-ink-secondary mt-2">Это read-only представление фактов API. Клиент не заявляет проверку хешей, которых нет в контракте.</p></header><section className="grid grid-cols-2 md:grid-cols-4 gap-3">{[["Фото Stage 2",photos.length],["Источник",s.data?.source_mode],["Стадия",photos[0]?.analysisStage],["Проверка",s.data?.not_a_verdict ? "не verdict" : "н/д"]].map(([k,v])=><div key={String(k)} className="rounded-lg border border-line-default bg-surface-base p-4"><div className="text-xs text-ink-muted">{k}</div><div className="mt-2 text-lg font-mono text-cyan-300">{v ?? "н/д"}</div></div>)}</section><section className="rounded-lg border border-line-default bg-surface-base p-5"><h2 className="font-mono text-xs text-cyan-300 mb-3">ПРОВЕРЯЕМЫЕ ПОЛЯ</h2><div className="grid md:grid-cols-2 gap-2 text-sm text-ink-secondary">{["photo_id", "date", "pose_bin", "quality", "flags", "measurement_status", "evidence_state", "stage2_pair_count"].map(x=><div key={x} className="rounded bg-surface-raised p-3 font-mono text-xs">{x} <span className="text-green-300">· из timeline API</span></div>)}</div></section><div className="rounded border border-amber-500 bg-amber-soft p-4 text-xs text-amber-300"><AlertTriangle className="inline h-4 w-4 mr-1"/>История действий пользователя и SHA-256-регистр не подключены к backend; старые синтетические записи удалены.</div></div>;
};
