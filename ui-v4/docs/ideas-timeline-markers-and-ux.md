# Таймлайн: 20 идей маркеров и визуальных элементов + 15 идей UI/UX

Два списка для следующих итераций интерфейса. Первый — про **отображение
данных на таймлайне** (маркеры, иконки, формы элементов). Второй — про
**общий UX**. Все идеи привязаны к текущему коду (ui-v4): таймлайн уже имеет
миниатюры, треки метрик, anomaly-полосу, playhead, A/B-режим и зум. Балл —
польза для расследования × наглядность × простота. S — часы, M — 1–2 дня.

---

## Часть 1 · 13 идей: слой «находок» на таймлайне (что реально важно)

Не метрики качества, а **результаты анализа**: выявленные аномалии, смещение
формы, смещение текстуры кожи, события возврата к предыдущему состоянию и
зоны избыточного копирования с предложениями чистки. Ядро этого слоя уже
реализовано (Iteration 07b: `/api/v1/timeline/findings` + переключатели
⚑ Аномалии / ⌁ Форма / ◈ Текстура / ▦ Плотность на таймлайне). Идеи ниже —
развитие и полировка.

| # | Идея | Балл | Статус |
|---|------|------|--------|
| 1 | Мосты смещения формы между соседями | 99 | ✅ сделано |
| 2 | Канал текстуры кожи (второй мост) | 97 | ✅ сделано |
| 3 | Флаги change-point ⚑ | 96 | ✅ сделано |
| 4 | События возврата ↩ с дугой к базовой линии | 95 | ✅ сделано |
| 5 | Зоны перекопирования + предложения чистки | 98 | ✅ сделано |
| 6 | Кластеры возвратов (к одному состоянию) | 94 | идея |
| 7 | «Скачок» — подсветка пар с аномальным темпом | 93 | идея |
| 8 | Легенда-сводка находок по бину | 92 | идея |
| 9 | Пунктир «в пределах шума» у спокойных участков | 91 | идея |
| 10 | Полоса калибровочной поддержки на панели | 90 | идея |
| 11 | Клик по мосту → карточка пары | 92 | идея |
| 12 | Авто-чистка зоны с предпросмотром | 93 | идея |
| 13 | Экспорт находок в отчёт | 90 | идея |

---

### 1. Мосты смещения формы между соседями · 99 · ✅

Между соседними фото в бине — дуга-мост: высота/толщина = величина смещения
формы (identity-only rmse), цвет = градиент зелёный→жёлтый→красный→бордовый,
толщина растёт с p95_z. Тревожные статусы (elevated, persistent, change
candidates) — непрозрачные и толстые, спокойные — полупрозрачные.

**Зачем.** Хронология формы читается как «кардиограмма»: где дуги становятся
толще и краснее — там и происходит изменение. Это главный визуальный ответ на
вопрос «когда и как менялась форма».

### 2. Канал текстуры кожи · 97 · ✅

Второй ряд дуг (ниже, своим цветом по тому же градиенту) — смещение в текстуре
кожи: delta качества текстуры пары, статус канала (measured/unavailable —
пунктир). Отдельный переключатель «◈ Текстура».

**Зачем.** Изменения кожи (рельеф, поры, текстура) — независимый канал
сравнения; его смещение может сигналить о том, что геометрия «не видит».

### 3. Флаги change-point ⚑ · 96 · ✅

Вертикальный флажок над датой, где активный run зафиксировал change point.
Красный — persistent / биологически-невероятный темп, жёлтый — candidate.
Тултип: дата, пара, p95_z, days_delta, rate-статус.

### 4. События возврата ↩ · 95 · ✅

Фото, вернувшееся к состоянию более ранней базовой линии (baseline return),
получает «↩»; если базовая линия есть в этом бине — пунктирная дуга от
возврата к ней. Цвет — по силе (opposite fraction).

**Зачем.** Возврат к предыдущему состоянию — одно из самых сильных наблюдений
в хронологии («форма изменилась, а потом вернулась к исходной»). Оно должно
читаться как событие, а не прятаться в json.

### 5. Зоны перекопирования + предложения чистки · 98 · ✅

Участки, где ≥6 фото за ≤90 дней: затенённая полоса «▦ ×7 · 45д · −3».
Фото-кандидаты на удаление (плохое качество, крайние позы, дубликаты)
получают красную пунктирную рамку и значок «−». Клик по зоне — панель со
списком причин и кнопкой **«Исключить N»** (применяется к текущей выборке).

**Зачем.** Избыточные копии за короткий срок = лишний шум и перекос пар.
Система предлагает, аналитик подтверждает — чистка прозрачна и воспроизводима.

### 6. Кластеры возвратов · 94

Если несколько событий ↩ возвращают к одной и той же базовой линии — собрать
их в один «веер» с общей точкой-якорем на базовом фото. Видно, сколько раз
форма возвращалась к одному состоянию.

### 7. «Скачок» — аномальный темп · 93

Пара с biologically improbable / rapid rate получает особый маркер «⚡» поверх
моста. Отличается от обычного превышения: это не «форма изменилась», а
«изменилась слишком быстро для биологии».

### 8. Легенда-сводка находок по бину · 92

В заголовке панели — мини-сводка: «⚑2 ↩1 ⚡3 · max rmse 0.018». Мгновенно
видно, в каком ракурсе вообще есть находки, не прокручивая ленту.

### 9. Пунктир «в пределах шума» · 91

Для пар, где всё в пределах калибровочного шума, мост рисуется тонким
пунктиром — «здесь стабильно». Контраст «пунктир vs жирная дуга» усиливает
восприятие аномалий.

### 10. Полоса калибровочной поддержки · 90

Вдоль низа панели — тонкая полоса с цветом бина по числу калибровочных персон
(✓ 7/7 — зелёная, 2/7 — жёлтая, 0 — серая). Выводы по слабо поддержанным
ракурсам визуально помечены ещё до анализа.

### 11. Клик по мосту → карточка пары · 92

Клик по любой дуге открывает карточку пары: оба фото, все значения shape и
texture, статусы, и кнопки «Открыть в Landmark Compare» / «Открыть в Morphing».

### 12. Авто-чистка с предпросмотром · 93

В панели зоны — переключатель «авто-чистка»: подбор удаляемых фото
пересчитывается при каждом изменении фильтров (качество, MAD-порог), с
предпросмотром «до/после» (сколько пар останется в бине).

### 13. Экспорт находок в отчёт · 90

Кнопка «Экспорт находок»: JSON/CSV со всеми мостами, флагами, возвратами и
зонами за период — для приложения к отчёту Stage 3.

## Часть 2 · 15 идей улучшения UI/UX

| # | Идея | Балл | Сложность |
|---|------|------|-----------|
| 1 | Undo/redo для всех действий | 98 | M |
| 2 | Rich-тултипы над фото | 97 | M |
| 3 | Контекстное меню (правый клик) | 96 | M |
| 4 | Фокус-режим (F11-таймлайн) | 95 | S |
| 5 | Тосты о фоновых задачах | 94 | S |
| 6 | Скелетоны и прогрессивная загрузка | 93 | S–M |
| 7 | Пресеты рабочего места | 92 | S–M |
| 8 | Статус-бар пайплайна | 92 | S |
| 9 | Табличные цифры (tabular-nums) | 91 | S |
| 10 | Цветобезопасная палитра | 90 | S–M |
| 11 | Пустые состояния с действием | 90 | S |
| 12 | Переключатель RU/EN | 89 | M |
| 13 | Экран горячих клавиш «?» + палитра | 89 | M |
| 14 | Режим презентации | 88 | S–M |
| 15 | Тёмная/светлая тема | 87 | M |

---

### 1. Undo/redo для всех действий · 98

Ctrl+Z / Ctrl+Shift+Z отменяют: движение ползунков фильтров, ручную курацию
профиля, A/B-выбор, настройки виджетов, переключение панелей. История —
в памяти сессии (до перезагрузки), в журнал профиля пишется только финальное
состояние.

**Зачем.** Случайное движение ползунка сейчас безвозвратно меняет выборку.
Отмена — базовая гигиена любого инструмента с ползунками.

### 2. Rich-тултипы над фото · 97

Ховер над миниатюрой — карточка: дата, ракурс, quality/confidence, pose-углы,
провенанс-статус, 2–3 аномалии, мини-превью face_crop и кнопки действий
(«Открыть», «Сравнить как A», «Исключить»). Появляется за 250 мс, не мешает
скроллу.

### 3. Контекстное меню (правый клик) · 96

Единая система контекстных меню: фото — «Photo Lab / как A / как B /
исключить из выборки / копировать ID / скриншот»; трек — «показать только его /
убрать / настройки порога»; панель — «только этот ракурс / скрыть». Ускоряет
рутину в 2–3 раза и разгружает панели инструментов.

### 4. Фокус-режим · 95

Клавиша F (или кнопка) — сайдбар и панели скрываются, таймлайн на весь экран.
Вторая F — возврат. Для долгих сессий сравнения и для скриншотов.

### 5. Тосты о фоновых задачах · 94

Уголок экрана с уведомлениями: «Run run_… завершён (225 пар)», «Отчёт готов»,
«Экспорт скачан», «Калибровка обновлена». Не перебивают работу, уходят сами
через 6 сек, клик открывает результат.

### 6. Скелетоны и прогрессивная загрузка · 93

Вместо «Загрузка…» — скелетон-заглушки формы карточек; миниатюры появляются
с blur-эффектом по мере загрузки (lazy). Большой датасет (сотни фото) не
выглядит «мёртвым» во время чтения.

### 7. Пресеты рабочего места · 92

Кнопки-пресеты: «1 ракурс», «3 ракурса + quality», «9 ракурсов + shift»,
«Провенанс», «Мой рабочий набор». Пресет сохраняет панели, треки, зум и
фильтры. Уже есть пресеты 1/3/9 — расширить их конфигурацией.

### 8. Статус-бар пайплайна · 92

Постоянная строка внизу: Stage 1 (N фото) · профиль (имя, N включено) ·
последний run (статус, время) · калибровка (7/7) · версия кода. Всегда видно,
на каких данных сейчас работаешь.

### 9. Табличные цифры · 91

`font-variant-numeric: tabular-nums` для всех чисел в треках, тултипах и
карточках. Значения не «прыгают» при обновлении — критично при сравнении
цифр на бегу.

### 10. Цветобезопасная палитра · 90

Проверка всех цветов на дейтеранопию (самый частый тип дальтонизма): градиент
смещений дублируется размером/формой точек (кольца, радиусы), статусы —
иконками, не только цветом. Легенда в шапке.

### 11. Пустые состояния с действием · 90

«Нет фото в этом ракурсе» → «Открыть Data Manager → Запустить извлечение»;
«Нет Stage 2» → «Run Manager → Запустить прогон (оценка пар: 225)». Каждое
пустое состояние — кнопка следующего шага, а не тупик.

### 12. Переключатель RU/EN · 89

Полный перевод интерфейса; forensic-термины (LDM, pose bin, calibration)
остаются английскими в обоих языках. Пригодится для внешних рецензентов и
публикаций.

### 13. Экран горячих клавиш «?» + палитра команд · 89

«?» открывает сводку хоткеев; Ctrl+K — палитра команд («фото…», «дата…»,
«ракурс frontal», «трек LDM106», «запустить Stage 2»). Частично уже есть
клавиатурная навигация — довести до единого слоя.

### 14. Режим презентации · 88

Кнопка «Презентация»: крупный шрифт, чистый фон, без служебных панелей, одна
панель на экран. Для показа редакции/рецензенту с проектора. Скриншоты из
этого режима сразу пригодны для документов.

### 15. Тёмная/светлая тема · 87

Переключатель темы (CSS-переменные уже есть). Светлая тема — для печатных
версий отчётов и скриншотов в текстовые документы; тёмная — основная рабочая.

---

## Рекомендуемый порядок

**Быстрые победы на таймлайне (1–2 дня):** №1 кольцо качества → №2 бейдж
ракурса → №3 конфликт дат → №6 границы эпох → №11 смайлы → №15 сектор
confidence. Таймлайн становится «читаемым» без треков.

**Глубина (2–3 дня):** №7 пропуски → №10 плотность → №8 ореол позы → №14
коннектор A→B → №5 флаги change-point (требует чтения активного run).

**UX (после маркеров):** №4 фокус-режим → №5 тосты → №1 undo/redo → №2
rich-тултипы → №3 контекстные меню.

Маркеры реализуются поверх текущих миниатюр (`pure-thumb`) и anomaly-полосы —
отдельный слой отрисовки, существующие элементы не ломаются.
