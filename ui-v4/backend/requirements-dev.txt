# DEV_FIX_TZ P3.21 — инструменты разработки (не нужны в проде).
# Установка:  pip install -r requirements-dev.txt
-r app6/api/requirements.txt
pytest>=8.0
httpx>=0.27          # требуется fastapi.testclient
ruff>=0.6            # статический анализ: заменяет flake8 + pyflakes + isort
