# AUDIT-5 — 50 финальных анализов: ТОП-20 ошибок в диффах и незатронутом коде

Дата: 2026-07-22 · Ветка: `arena/019f89cd-facproject` · Фокус: (а) ошибки самих диффов (патчей PR.md и их применения), (б) ошибки в коде вне диффа, (в) незамеченные, дублирующиеся и неактуальные функции.

Вердикты: **PASS** · **FIXED** · **NOTE** · **FAIL**.

---

## Блок A. Дубли / shadowing / неактуальные функции (A1–A10)

| # | Анализ | Вердикт | Детали |
|---|---|---|---|
| A1 | Shadowing `def` на уровне модуля | **PASS** | 0 одинаковых имён функций, перекрывающих друг друга в одном модуле. |
| A2 | Кросс-модульные дубли публичных имён (3+ модуля) | **PASS** | Только `add` (3 разные роли: leads/preflight/release-gate) — не конфликт. |
| A3 | Пары дублей `sha256_file`/`atomic_json` (utils vs skin.serialization) | **NOTE** | Две сознательно разные реализации: skin-версия строже (fsync, allow_nan=False, SafeEncoder). Не баг; кросс-ссылки есть в docstring'ах. |
| A4 | Дубли атласа legacy ↔ final | **FIXED** | Реальность была ИНВЕРТИРОВАНА в доке: `skin_zone_atlas_final.py` (471 стр.) не импортируется НИЧЕМ (ни проде, ни тестах) → помечен `⚠️ NOT WIRED` с точным live-путём (`skin/projection.py`); legacy оставлен как живой генератор каноники. |
| A5 | `load_canonical_atlas`/`project_atlas_to_photo` в обоих атласах | **NOTE** | Парные реализации преднамеренны (legacy-генератор vs кандидат v3). После A4 статусы у каждой явно подписаны. |
| A6 | Статус функций `in_progress`, реально подключённых в прод | **PASS** | `uv_geometry_pair` вызывается из engine — статус честно отражает «адаптер без калибровки», не ошибка. |
| A7 | `apply_biological_rate_flags` — deprecated-алиас | **FIXED** | Алиас молча делегировал → теперь логирует `deprecated` при вызове, делегация сохранена для совместимости. |
| A8 | Мёртвые enum-записи `*_profile_soft` в motion.py | **FIXED** | `config.POSE_BINS` никогда не эмитит такие имена — недостижимые ветки удалены; fallback `"profile" in p` покрывает будущие названия. |
| A9 | «Dead-кандидаты» (49 шт наивным сканом) | **PASS** | Ручная проверка: все — false positives (методы классов, внешний API, вызовы через строки-реестры, тестовые входы). Реальный dead code — только A4/F1. |
| A10 | `public_dict()` без вызывающих | **NOTE** | Публичный dataclass-хелпер Stage1Config — оставлен как API-поверхность. |

## Блок B. Артефакты применения диффов (B1–B10)

| # | Анализ | Вердикт | Детали |
|---|---|---|---|
| B1 | Дублированные вызовы `log_status()` (двойное нанесение хуков) | **FIXED** | Найдено и удалено **7** идентичных соседних вызовов в `stage1/geometry.py` (classify_pose, nearest_canonical_yaw, full_pose_correction_matrix, normalize_mesh, normalize_mesh_landmark_anchored, compute_chronology_alignment, to_original_image). Каждый вызов логировал одно и то же дважды. |
| B2 | Обобщённый скан идущих подряд идентичных стейтментов | **PASS** | Кроме B1 — только легитимные `return "cpu"`/`return 0.0` в разных ветках. Повторное нанесение других хунков не обнаружено. |
| B3 | Дубли импортов после патчей | **PASS** | Дублирующихся import-строк нет. |
| B4 | Дубли ключей в dict-литералах (тихий override!) | **PASS** | AST-скан: 0 словарей с повторными ключами во всём app6 (проверка именно дифф-стиля записи info.json). |
| B5 | Дубли определений констант фильтров | **PASS** | `MIN_ALIGNMENT_QUALITY=0.5`, `MAX_EXPRESSION_MAGNITUDE=1.5` — ровно по одному определению. |
| B6 | Рассинхрон имён log_status и реальной функции | **PASS** | Скан всех `log_status("name")` внутри `def` — 0 несовпадений (copy-paste патчей чист). |
| B7 | Битые локальные импорты из патч-дампа | **PASS** | Ранее исправлены в AUDIT-2; новых не появилось (компиляция 156/156). |
| B8 | Стирающиеся trailing-space строки из дампа | **PASS** | Устранено ещё при применении; регрессий нет. |
| B9 | Повреждённые markdown-таблицы отчётов из дампа | **PASS** | Отчёты переписаны нативно (AUDIT-2..4); дамп не рассматривается как источник. |
| B10 | Двойные docstring из патчей | **PASS** | Формат `'''` в CONVENTIONS — единственный сознательный, задокументирован. |

## Блок C. Ошибки в коде вне диффа (C1–C10)

| # | Анализ | Вердикт | Детали |
|---|---|---|---|
| C1 | Undefined-name риски (symtable-скан) | **PASS** | 1 ложное (`__class__` в методе — compiler-provided). Реальных NameError-рисков 0. |
| C2 | Dead store `ldm134_original` | **FIXED** | Присваивание без потребителя удалено (`to_original_image` для 134-х ландмарков не нужен пайплайну) + 💡 NOTE как вернуть симметрично. |
| C3 | Неиспользуемые `lo`/`hi` в `nearest_canonical_yaw` | **FIXED** | Переименованы в `_lo/_hi` с пояснением (выбор по близости к canonical, границы не нужны). |
| C4 | Проверка `errors`/`pose_counts`/`seen_hashes` в engine.run() | **PASS** | Все используются (false positive наивного скана): дедупликация по хэшу, хроно-индексация, errors.csv. |
| C5 | Контракт renderer (`visible_vertice` kwarg vs positional) | **PASS** | `3ddfa_v3/model/recon.py:480` вызывает строго keyword → перехват в `renderer_forward` корректен; баг отсутствует. |
| C6 | Ветки `try/except` без логирования | **PASS** | Все стратегические `except` имеют fallback-логику (skin graceful degradation). |
| C7 | Bare except в stage3/engine.py | **NOTE** | 1 место — рендер-фолбек отчёта; поведение приемлемо, помечено. |
| C8 | NaN-риски в статистических примитивах | **PASS** | Где критично — есть np.isfinite валидатор и reprojection guards. |
| C9 | Индексные риски (_resolve_mesh_count) | **PASS** | Динамическая подстройка под mesh_count на месте. |
| C10 | Дата-разбор `r.date or '9999'` сортировок | **PASS** | Консистентен между stage2 engine и motion/chronology. |

## Блок D. Семантика диффов (D1–D10)

| # | Анализ | Вердикт | Детали |
|---|---|---|---|
| D1 | Фильтры качества/мимики при ОТСУТСТВИИ info.json | **FIXED** | Дефолты `.get(id, 1.0|0.0)` молча пропускали такие записи через фильтр (loss of filtering без сигнала). Добавлены счётчики `missing_alignment/missing_expression` + `status_warning` — фильтрация наблюдаема, поведение фильтра не изменено. |
| D2 | Счётчики skipped_alignment/expression попадают в отчёт | **NOTE** | По патч-замыслу — только консоль; оставлено (минимальный контракт). Наблюдаемость улучшена в D1. |
| D3 | `nearest_canonical_yaw` не интегрирован | **NOTE** | Так и заявлено («Not integrated», IN PROGRESS) — осознанный задел; статусы согласованы (STATUS_AUDIT ↔ docstring ↔ FUNCTION_STATUS_LOG). |
| D4 | `main_index.csv` = побайтовый дубль main_timeline.csv | **FIXED(док)** | Потребителей в app6 нет (stage2 читает только main_timeline) — задержан как compat-alias стиля app7, факт задокументирован в коде. |
| D5 | `to_original_image` без bounds-check | **NOTE** | Помечено IN PROGRESS осознанно: clamp изменит координаты ассетов (контракт) — не трогаем до калибровки. |
| D6 | `expression_magnitude` при битом info.json | **PASS** | `except Exception` → запись просто отсутствует; D1 делает это наблюдаемым. |
| D7 | Счётчики quality/calibration в payload | **PASS** | payload включает метрики флагов и каталог (engine.py:279). |
| D8 | Согласованность схемы v2.4 | **PASS** | Строка схемы едина в engine/validator/tests. |
| D9 | Патч-физика поворотов (знаки углов) | **PASS** | Golden-тесты 5/5 + standalone 7/7 подтверждают корректность отклонения от дампа. |
| D10 | Форматирование гигантской строки engine.py:228 | **NOTE** | Патч-стиль; читаемость страдает, но поведенчески чисто — не рефакторим в этом аудите. |

## Блок E. Документационная правдивость (E1–E10)

| # | Анализ | Вердикт | Детали |
|---|---|---|---|
| E1 | Docstring `export_atlas_correspondence.py` врёт о зависимости | **FIXED** | Скрипт — чистый npz→npz соответствователь треугольников (LUT вершин), НЕ импортирует атласы; описание моего AUDIT-4 исправлено на точное (FAIL-LOUD coverage/bijectivity). |
| E2 | Статус «актуальный» у skin_zone_atlas_final | **FIXED** | См. A4 — помечен NOT WIRED, точный live-путь указан. |
| E3 | Legacy-атлас заявлен «вызываемой из assets.py» | **FIXED** | Ссылка устарела (assets.py явно запрещает legacy-fallback) — строка docstring исправлена, живые потребители перечислены. |
| E4 | STATUS_AUDIT ↔ код | **PASS** | Реестра валиден (B10 аудита-4 + этот прогон). |
| E5 | FUNCTION_STATUS_LOG свежесть | **FIXED** | Перегенерирована после правок. |
| E6 | AUDIT-4 утверждения не противоречат AUDIT-5 | **PASS** | Расхождение (E1/E2) разрешено правками в коде; отчёт AUDIT-4 остаётся историческим логом. |
| E7 | Комментарии патч-констант (jaw/expression) | **PASS** | Пороги прокомментированы у места определения. |
| E8 | TODO-стыки патчей с реальностью | **PASS** | Все 8 TODO соответствуют коду. |
| E9 | Описания ролей 🚪/🎯 после правок | **PASS** | Перепроверены в затронутых файлах. |
| E10 | Язык новых правок | **PASS** | Русский, стиль CONVENTIONS. |

---

## ТОП-20 ошибок (итоговый рейтинг)

**Исправлено (10):**
1. **B1** — 7 дублированных `log_status()` в geometry.py (артефакт двойного нанесения патчей) — двойное логирование каждого вызова.
2. **A4/E2** — `skin_zone_atlas_final.py` незамеченный мёртвый модуль (471 стр.) с инвертированным статусом «актуальный» в доке.
3. **D1** — фильтры alignment/expression молча теряли силу при отсутствии info.json (loss of filtering без сигнала).
4. **C2** — dead store `ldm134_original` в engine.py.
5. **A8** — недостижимые `*_profile_soft` ветки в motion.py.
6. **E1** — docstring export_atlas_correspondence ошибочно ссылался на atlas-модуль.
7. **E3** — устаревшая ссылка «из assets.py» в legacy-атласе (запрещено явным fallback-ban).
8. **A7** — deprecated-алиас не логировал свой статус.
9. **C3** — неиспользуемые раскрытия `lo/hi` (путаница намерений).
10. **D4** — незадокументированный дубль main_index.csv (теперь объяснён как compat-alias).

**Задокументировано как осознанное (10):**
11. D3 — `nearest_canonical_yaw` не интегрирован (IN PROGRESS по замыслу).
12. A3 — парные реализации sha256_file/atomic_json (слои-специфичные).
13. D5 — отсутствие bounds-check в to_original_image (контракт координат).
14. D2 — счётчики фильтров только в консоль (патч-контракт).
15. sys.path-хаки скриптов/адаптеров (24 места, packaging-техдолг — из AUDIT-4 D3).
16. C7 — bare except в stage3 (рендер-fallback).
17. D10 — монолитная строка engine.py:228 (стиль патча).
18. A10 — неиспользуемый public_dict как API-поверхность.
19. Внешний ассет `texture_zones_bfm35709_v3.npz` (test_wrinkle_zones — out-of-scope).
20. uv_module: single-public-render рефактор (out-of-scope, вне app6).

**Проверки после исправлений:** компиляция 156/156; `pytest app6/tests`: **78 passed** без регрессий (те же 6+4 out-of-scope); golden 5/5; standalone 7/7; geometry+pose 11/11.
