# Итоговый план доработок до 100%

Дата: 2026-07-29 · Ветка: `arena/019fab05-facproject`
Источники: аудит 74 проверок (`AUDIT_70_FINDINGS.md`), `REQUIREMENTS_FOR_DEVELOPER.md`, `FINAL_UI_SPECIFICATION.md`, `private_hypothesis_seed/`.

Цель: пайплайн на MacBook M1 (CPU-only, 3DDFA_V3 + OpenCV) для хронологического анализа фото 1999–2026 в 9 ракурсах.

---

## 1. Текущая готовность

### Общий процент

| Блок | Готово | Комментарий |
|---|---:|---|
| Геометрическое ядро | **95%** | Валидировано: AUC 0.998–1.000 по всем 9 ракурсам |
| Инфраструктура Stage 1 | **70%** | Код есть, не запускается: нет весов и guard |
| Stage 2 (анализ) | **75%** | 31 модуль, 41 артефакт; 7 дефектов искажают результат |
| Stage 2B / Stage 3 | **60%** | Работают, но непроверяемы без Stage 1 |
| Атлас зон | **40%** | 11 зон из 23 мертвы, двойной учёт носа |
| Текстура / аутентичность | **45%** | Модель one-class, 3 модуля ТЗ не начаты |
| Тестовый контур | **0%** | `test_module` удалён, 0 тестов в app6 |
| UI | **0%** | Только спецификация и 3 прототипа с мок-данными |
| Legacy-интеграция | **20%** | 6223 записи импортированы, 0 ретестировано |
| **ИТОГО** | **≈52%** | |

### Что готово и менять не нужно

Подтверждено исполнением кода:

- **Разделимость личностей.** `frontal`: свой/чужой = 0.0035 / 0.0658, отношение 18.9×, AUC = 1.000. По всем 9 ракурсам AUC ≥ 0.998. Метод пригоден для задачи.
- **Kabsch-выравнивание.** Ошибка 1.9e-07, подавляет 10% выбросов, `det(R)=+1` (без отражений), метрика симметрична.
- **Калибровка грузится.** 943 записи, 7 персон, 9 бинов без перекрытий, `canonical_yaw` согласован, 0 битых ссылок, 0 дублей. `CalibrationModel` строится за 11.8 с.
- **BH-процедура математически корректна.** Монотонна, 0/100 ложных, 20/20 истинных, пустой вход безопасен.
- **Хронология.** Битые даты → `date_missing`, same-day и CUSUM срабатывают корректно.
- **Сериализация и атомарность.** `json_ready` (NaN/Inf → null, numpy-типы), `atomic_json`/`write_csv` через `os.replace` + UUID.
- **Детерминизм.** Повтор идентичен, `random` без seed отсутствует.
- **Безопасность.** `eval`, `exec`, `shell=True`, `yaml.load`, небезопасный `pickle` в рабочем коде **отсутствуют**.
- **Именование.** `parse_photo_name` нормализует legacy-формы `2019_03_18 (2).jpg` → `sequence=2`.

---

## 2. Статус 17 пунктов ТЗ

| № | Пункт | Приоритет | Статус |
|---:|---|---|---|
| 1 | Вычитание углового шума | P0 | **не начато** |
| 2 | Исключение зон мимики | P0 | **не начато** |
| 3 | 21 костная зона | — | **отменён пользователем** |
| 4 | Аномалия A→B→A | P0 | **не начато** |
| 5 | FFT микрорельефа | P1 | **не начато** |
| 6 | LBP-сложность | P1 | **не начато** |
| 7 | Анализ альбедо | P1 | **не начато** |
| 8 | Компенсация качества | P1 | **не начато** |
| 9 | FDR Benjamini-Hochberg | P2 | **частично** — `_bh_qvalues` корректна, но `q=0.10` вместо 0.05, приватная |
| 10 | Same-day конфликт | P2 | **частично** — эвристика есть, гейта 3σ нет |
| 11 | Хеш-гарды | P2 | **частично** — считаются, не сверяются |
| 12 | Атомарная запись | P2 | **готово** — `write_csv` уже атомарна |
| 13 | `yaml.safe_load` | P2 | **готово** — 0 вхождений `yaml.load` |
| 14 | Контроль заголовков CSV | P2 | **не начато** |
| 15 | 3D Inspector | P3 | **не начато** |
| 16 | Scenario Planner | P3 | **не начато** |
| 17 | Fix Capsule | P3 | **не начато** |

Готово 2 из 16 актуальных, частично 3, не начато 11.

---

## 3. План работ

### ЭТАП 0 — Разблокировка (обязателен первым)

Без него ничего нельзя проверить.

| # | Задача | Проблема |
|---|---|---|
| 0.1 | Восстановить `app6/test_module/pipeline_guard.py` | B1: все 4 точки входа падают с `ModuleNotFoundError` |
| 0.2 | Каркас pytest + conftest | 0 тестов в app6; критерий ТЗ «87 тестов» недостижим |
| 0.3 | Адаптер архива `selected_photos_7x9x3` в Stage-1-подобный вход | 188 записей, 7 персон × 9 ракурсов — единственные реальные данные |
| 0.4 | Убрать абсолютные пути | D13: `run_calibration.py --help` падает |
| 0.5 | Починить импорт 2 скриптов | D14: `parse_args()` на уровне модуля убивает процесс |

Результат: `pytest` запускается, Stage 2 работает на архиве.

### ЭТАП 1 — Дефекты, искажающие результат

| # | Задача | Дефект |
|---|---|---|
| 1.1 | NaN/Inf → статус `not_measurable` в `calibrated_score` | **D1**: NaN даёт `elevated` на каждой паре через `alpha_*_l2` |
| 1.2 | Перегенерировать `mesh_zone_indices.json` | **D2**: 11 зон из 23 < 4 вершин (`chin`=1, виски=2) |
| 1.3 | Устранить вложенность `nose_wing_*` ⊂ `nose_bridge_tip` | **D3**: 589 вершин считаются дважды |
| 1.4 | Подключить `pose_policy_v3_9bins.csv` в рантайм | **D4**: нормативный атлас не читается кодом |
| 1.5 | Зафиксировать конвенцию знака yaw, тест-инвариант | **D5**: 96/180 ячеек инвертированы между версиями |
| 1.6 | `balanced_reference`: минимум 3 персоны | **D6**: принимает 1 человека |
| 1.7 | Синхронизировать `ZONE_WEIGHTS` с реальностью | **D11**: координатная сетка названа «bone zones» |

Каждая задача — с regression-тестом, который падает до фикса.

### ЭТАП 2 — P0 из ТЗ

| # | Задача | Обоснование |
|---|---|---|
| 2.1 | п.1 `subtract_angle_noise` | Измерено: корреляция 0.463, RMSE растёт в 2.6× при росте Δугла |
| 2.2 | п.2 `exclude_mimic_zones` | Мимика искажает мягкие ткани |
| 2.3 | п.4 `detect_irreversible_return` | A→B→A — ядро гипотезы расследования |
| 2.4 | Взвешивание по **24 legacy-зонам** вместо 21 новой | Готовая валидированная схема из ledger |

### ЭТАП 3 — Мост к legacy (6223 гипотезы)

| # | Задача | Проблема |
|---|---|---|
| 3.1 | Таблица соответствия ракурсов legacy ↔ current | **D12**: 6 из 9 бинов не сматчатся |
| 3.2 | Реестр 80 legacy-метрик как целей ретеста | Пороги пересчитываются, старые не наследуются |
| 3.3 | Проверка гипотезы эпох (1999–2012 / 2012–2021 / 2021–2023 / 2023–2026) | Готовая разметка в `era_breakpoints` |

Граница: legacy — цели перепроверки, не истина. Не нарушать.

### ЭТАП 4 — P1/P2 из ТЗ

| # | Задача |
|---|---|
| 4.1 | п.5 FFT, п.6 LBP, п.7 альбедо (skimage уже доступен) |
| 4.2 | п.8 компенсация качества (1999 VHS vs 2025 HD) |
| 4.3 | п.9 публичная `benjamini_hochberg`, FDR = 0.05 (**D7**) |
| 4.4 | п.10 same-day гейт 3σ |
| 4.5 | п.11 `verify_integrity_hashes` в preflight (**D8**) |
| 4.6 | п.14 `validate_csv_headers` + стабильный порядок колонок (**D9**) |
| 4.7 | Переименовать метрику силикона, зафиксировать ограничение (**D10**) |

### ЭТАП 5 — Прогон на реальных данных

Лестница из `AGENTS.md`, без пропусков:

```
1 кадр → 10 фото → 100 фото → полный набор
```

| # | Задача | Блокер |
|---|---|---|
| 5.1 | Получить веса: `net_recon.pth`, `large_base_net.pth`, `retinaface_*.pth`, `similarity_Lm3D_all.mat` | **B2**: в `assets/` только `face_model.tar.gz` |
| 5.2 | Установить torch (CPU, Apple Silicon) | не установлен |
| 5.3 | Подключить фотодатасет 1999–2026 | в репозитории 0 фото |
| 5.4 | Stage 1 → Stage 2 → Stage 3 по лестнице | |

**Требует вашего участия** — весов и фото в репозитории нет.

### ЭТАП 6 — UI

| # | Задача | Проблема |
|---|---|---|
| 6.1 | Исправить ошибки в `FINAL_UI_SPECIFICATION.md` | Раздел 12.3 описывает `frame_000205` как `frontal_0, yaw=-2.3`; реально `left_profile, yaw=-79.57` |
| 6.2 | Заменить `PoseBucket` (5 значений) на 9 реальных бинов | В данных нет `frontal_yaw15`/`profile_L` |
| 6.3 | Решить судьбу H0/H1/H2 в UI | Конфликт с `AGENTS.md`: запрещён категоричный вердикт |
| 6.4 | Бэкенд под 24 эндпоинта | `ui/` удалён |
| 6.5 | Фронтенд по спецификации | 3 прототипа на мок-данных |

---

## 4. Порядок и зависимости

```
ЭТАП 0 (разблокировка)
   ├─> ЭТАП 1 (дефекты)  ─┬─> ЭТАП 2 (P0 ТЗ)
   │                      └─> ЭТАП 3 (legacy)
   └─> ЭТАП 4 (P1/P2)
              │
              └─> ЭТАП 5 (реальные данные) ── требует весов+фото от вас
                         │
                         └─> ЭТАП 6 (UI)
```

Этапы 0–4 выполнимы в текущем окружении на архиве и калибровке.
Этап 5 блокирован отсутствием весов и фотодатасета.
Этап 6 зависит от 5 (нечем наполнять UI).

---

## 5. Определение «100% готово»

- [ ] Все 4 точки входа запускаются
- [ ] pytest: ≥3 теста на каждый новый модуль, все зелёные
- [ ] Ни одной зоны атласа < 4 вершин, нет вложенных зон
- [ ] Нефинитные значения никогда не дают `elevated`
- [ ] Нормативный атлас поз читается рантаймом, конвенция yaw закреплена тестом
- [ ] 15 актуальных пунктов ТЗ реализованы (п.3 отменён)
- [ ] FDR = 0.05, хеш-гарды блокируют запуск при расхождении
- [ ] 6223 legacy-гипотезы ретестируемы (не `pending` из-за имён)
- [ ] Лестница 1 → 10 → 100 → полный набор пройдена без structural errors
- [ ] Ни одного категоричного вердикта о личности в публичном выводе
- [ ] UI-спецификация не противоречит фактическим данным

---

## 6. Что нужно от вас

| # | Нужно | Зачем | Блокирует |
|---|---|---|---|
| 1 | Веса 3DDFA_V3 (4 файла) | Stage 1 не запустится | Этап 5 |
| 2 | Фотодатасет 1999–2026 | В репозитории 0 фото | Этап 5 |
| 3 | Решение по H0/H1/H2 в UI | Конфликт с `AGENTS.md` | Этап 6 |
| 4 | Судьба `test_module` | Восстановить или tests/ | Этап 0 |

---

## 7. Рекомендуемый первый шаг

Этапы 0 и 1: разблокировать пайплайн и устранить 7 дефектов, искажающих результат. Это выполнимо здесь и сейчас на калибровке и архиве, не требует ни весов, ни фото, и делает все последующие измерения достоверными.
